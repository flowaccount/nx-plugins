import { BuilderContext, BuilderOutput } from '@angular-devkit/architect';
import { ServerlessBaseOptions } from './types';
export default function copyAssetFiles(options: ServerlessBaseOptions, context: BuilderContext): Promise<BuilderOutput>;
